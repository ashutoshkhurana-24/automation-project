{
  "name": "the-house-shell",
  "version": "1.0.0",
  "private": true,
  "description": "Android shell around the dashboard the hub already serves",
  "scripts": {
    "apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
